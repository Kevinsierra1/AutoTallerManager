-- AutoTallerManager Database Initialization
-- This script runs before EF migrations for Docker initialization

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- The schema is managed by EF Core migrations.
-- Run: dotnet ef database update
-- Or use the API startup migration on first run.
